import { Step } from '../../../../_models';

export class DivisionResultFormatter {
  private _dividend: string;
  private _divisor: string;
  private _quotient: string;
  private _remainder: string;
  private _stps: Step[];

}
